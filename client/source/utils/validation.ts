// import { ValueType } from '../types';

// export const validation = (value: ValueType, rules: {(v: ValueType) => string}[]):string [] => {
//   return rules.map((rule) => {
//     return rule(value);
//   })
// };

// export const checkValidation = (arr: string[]):boolean => {
//   return !arr.some(v => v !== '');
// };
