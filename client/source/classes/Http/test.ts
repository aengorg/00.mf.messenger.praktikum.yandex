import { HTTPTransport } from './Http';

const BASE_URL = 'https://jsonplaceholder.typicode.com';

describe('HTTPTransport', () => {
  it('get', async () => {
    //   const http = new HTTPTransport();
    //   const data = await http.get(`${BASE_URL}/todos/1`);
    //   console.log(data);
    expect(true);
  });

  it('get params', async () => {
    expect(true);
  });

  it('post', async () => {
    expect(true);
  });

  it('delete', async () => {
    expect(true);
  });
});
