import { Route } from './Route';

describe('Route', () => {
  it('монтируем страницу', () => {
    expect(true);
  });

  it('инициализируем страницу', () => {
    expect(true);
  });

  it('очищаем страницу', () => {
    expect(true);
  });
});
