import { Router } from './Router';

describe('HashRouter', () => {
  it('добавляем путь', () => {
    expect(true);
  });

  it('удаляем путь', () => {
    expect(true);
  });

  it('переходим по пути', () => {
    expect(true);
  });

  it('переходим назад', () => {
    expect(true);
  });

  it('переходим вперед', () => {
    expect(true);
  });
});
